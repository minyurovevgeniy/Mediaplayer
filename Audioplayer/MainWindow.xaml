<Window x:Class="Audioplayer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Audioplayer"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="347*"></RowDefinition>
            <RowDefinition Height="87*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Button Content="Open" x:Name="chooseFile" Grid.Column="0" Grid.Row="0" Click="chooseFile_Click" Margin="10,272,10,10" />
        <TextBlock Grid.Column="1" Grid.Row="0" x:Name="headerBlock" TextWrapping="WrapWithOverflow" Margin="40,0,40,0" />
        <MediaElement  x:Name="myMediaElement" 
                  MediaFailed="Media_MediaFailed"
                  MediaOpened="Media_MediaOpened"
                  MediaEnded="Media_MediaEnded" 
                  LoadedBehavior="Manual" 
              UnloadedBehavior="Manual" Grid.ColumnSpan="2" Margin="0,0,266,0" />
        <Button Grid.Column="0" Grid.Row="1" Content="Play" Click="Play_Click" Margin="10,10,10,10" />
        <Button Grid.Column="1" Grid.Row="1" Content="Pause" Click="Pause_Click" Margin="10,10,10,10"/>
        <Button Content="Stop" Grid.Column="2" Grid.Row="1" Click="Stop_Click" Margin="10,10,10,10" />
        <ListBox x:Name="audioList" Grid.Column="2" Grid.Row="0"/>
    </Grid>
</Window>
